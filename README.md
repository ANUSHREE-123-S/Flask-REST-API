# Flask REST API – User Management

## 📌 Objective
Build a simple REST API using **Flask** that performs CRUD operations (Create, Read, Update, Delete) on user data.

## 🛠️ Tools Used
- Python 3.x  
- Flask  
- Postman / cURL for API testing  

Features

In-memory user storage

Full CRUD API

Simple, beginner-friendly Flask structure
